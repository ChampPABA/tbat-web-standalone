{
  "framework": "nextjs",
  "buildCommand": "prisma generate && npm run build",
  "installCommand": "npm install --legacy-peer-deps",
  "functions": {
    "app/**/*.{ts,tsx}": {
      "maxDuration": 10
    }
  }
}